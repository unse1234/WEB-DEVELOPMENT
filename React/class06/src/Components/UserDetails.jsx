import React from 'react'
import { useParams } from 'react-router-dom'

const UserDetails = () => {
    // const { id } = useParams()
   
    // const foundUser = user.find(u => u.id === parseInt(id))

  return (
    <div>qw</div>
//     <div>
//         <h1 className=' text-6xl font-bold '>User Details</h1>
// <h2>{foundUser?.name || "User Not Found"}</h2>
//       <p>{foundUser?.email}</p>
//     </div>
  )
}

export default UserDetails
